"""News sources for the self-updating scraper.

Every source module exposes a `fetch(...)` function returning List[Article].
Add a new source by dropping a file here and importing it from run_daily.py.
"""
